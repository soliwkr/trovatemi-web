const screens=[...document.querySelectorAll('[data-screen]')];const dots=[...document.querySelectorAll('[data-jump]')];let current=0;function show(index){current=Math.max(0,Math.min(index,screens.length-1));screens.forEach((s,i)=>s.classList.toggle('active',i===current));dots.forEach((d,i)=>d.classList.toggle('active',i===current));window.scrollTo({top:0,behavior:'smooth'});}document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>show(Number(b.dataset.next))));dots.forEach(d=>d.addEventListener('click',()=>show(Number(d.dataset.jump))));document.getElementById('restart')?.addEventListener('click',()=>show(0));document.querySelector('.restart-bottom')?.addEventListener('click',()=>show(0));document.addEventListener('keydown',e=>{if(e.key==='ArrowRight')show(current+1);if(e.key==='ArrowLeft')show(current-1);});